<button {...Astro.props}>
  <slot />
</button>
<style>
  button {
    --bg-color: #f5f5f5;
    --main-color: #0a0a0a;
    font-family: 'Quicksand';
    background-color: var(--bg-color);
    color: var(--main-color);
    font-weight: bolder;
    text-align: center;
    width: 75px;
    text-decoration: none;
    border: 2px solid var(--main-color);
    border-radius: 10px;
    box-shadow: 2px 4px 0px var(--main-color);
    transition: all 0.3s ease;
    cursor: pointer;
  }

  button:hover {
    background-color: #fff;
    color: #4338ca;
    border: 2px solid #818cf8;
    box-shadow: 5px 5px 0px #818cf8;
  }

  button:active {
    background-color: #fcf414;
    box-shadow: none;
    transform: translateY(4px);
  }
</style>
