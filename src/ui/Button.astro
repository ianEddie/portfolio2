<button {...Astro.props}>
  <slot />
</button>
<style>
  button {
    --bg-color: #f5f5f5;
    --main-color: #0a0a0a;
    font-family: 'Quicksand';
    background-color: var(--bg-color);
    color: var(--main-color);
    font-weight: bolder;
    text-align: center;
    width: 75px;
    padding-block: 1px;
    text-decoration: none;
    border: 2px solid var(--main-color);
    border-radius: 10px;
    box-shadow: 2px 4px 0px var(--main-color);
    transition: all 0.3s ease;
    cursor: pointer;
  }

  button:hover {
    border: 2px solid #d4d4d4;
    box-shadow: 5px 5px 0px #d4d4d4;
    @apply bg-indigo-950 text-amber-100;
  }

  button:active {
    background-color: #fcf414;
    box-shadow: none;
    transform: translateY(4px);
  }
</style>
