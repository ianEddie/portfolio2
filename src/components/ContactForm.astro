<form>
  <div>
    <label>Nombre</label>
    <input type='text' placeholder='ingrese su nombre' autofocus required />
  </div>
  <div>
    <label>E-mail</label>
    <input type='text' placeholder='ingrese su mail' required />
  </div>
  <div>
    <label>Consulta</label>
    <textarea placeholder='Escriba su consulta'></textarea>
  </div>
  <button>Enviar</button>
</form>
<style>
  form {
    font-family: 'Outfit Variable', sans-serif;
    @apply bg-indigo-700 py-10 rounded-3xl w-1/4 flex flex-col items-center gap-y-5 border border-indigo-400 shadow-xl shadow-indigo-900;
  }
  div {
    @apply flex flex-col items-center gap-y-3 w-full;
  }
  label {
    @apply text-pink-100 font-medium tracking-wide;
  }
  textarea {
    @apply bg-indigo-500/70 w-2/3 rounded-lg text-center py-1;
  }
  input {
    @apply w-2/3 rounded-full  bg-indigo-500/70 border border-indigo-400 py-1 text-center text-white;
  }
  textarea::placeholder {
    @apply text-yellow-100/60 italic;
  }
  input::placeholder {
    @apply text-yellow-100/60 italic;
  }
  button {
    @apply bg-azul/70 font-medium px-5 rounded-full py-1 shadow-lg shadow-indigo-900/80;
  }
</style>
